/**
 * set_string - set the value of pointer to string
 * @s: pointer to pointer to a string
 * @to: pointer to a character to set to the string
 * pointer
 * Return: nothing
 */

void set_string(char **s, char *to)
{
	*s = to;
}
